{
  "name": "sodilame-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine SODILAME — cuisines professionnelles, Saint-Martin-de-Crau",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx --yes serve public -l 3000",
    "preview": "npx --yes serve public -l 3000",
    "check": "node build.mjs && node check.mjs",
    "test": "node build.mjs && node tests/catalogue.mjs && node tests/sav.mjs && node tests/e2e.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "pg": "^8.23.0"
  }
}
